{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Debug tests",
            "type": "bun",
            "request": "launch",
            "runtimeArgs": ["test"],
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "noDebug": false,
            "runtime": "bun"
        }
    ]
}